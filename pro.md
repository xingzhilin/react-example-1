# React的使用
	webpack + babel + babel-loader+ ES2015 + react

# npm init 

# npm install webpack -g  -- npm install webpack --save-dev

# webpack.config.js. webpack2
	2.x - 直接的入口和出口

# 安装 babel-loader 
	-  github上 babel-loader

	
	npm install babel-loader
	npm install babel-core
	npm install babel-predet-es2015
 

 运行webpack 

 # 配置react

   npm install react --save-dev
   npm install react-dom --ave-dev

   npm install babel-predet-react --save-dev 
   配置babel-loder

   

